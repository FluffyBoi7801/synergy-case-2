{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "set NODE_ENV=develop && npx prisma generate && npx prisma migrate deploy && nodemon src/index.ts",
    "build": "tsc",
    "start": "set NODE_ENV=production && npx prisma generate && npx prisma migrate deploy && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Artem Kuchinskii",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "dotenv": "^17.2.1",
    "eslint": "^9.32.0",
    "nodemon": "^3.1.10",
    "prettier": "^3.6.2",
    "prisma": "^6.13.0",
    "prisma-json-schema-generator": "^5.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
